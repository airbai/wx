<view class="w">
   <!--  <view class="case pr">
        <image class="pic"data-gid="1000102"mode="aspectFill"src="http://i1.carimg.com/0/photo/012/085/000/1715820348a92783.jpg!hmm"></image>
        <view class="title ellipsis">波普两居 明悦和煦</view>
        <view class="tag ellipsis">二居室 | 120平米以下 | 20万左右</view>
    </view>
    adsfadfadsf -->
    <view wx:for="{{data}}" wx:key="id" class="case pr">
        <image class="pic" src="{{item.cover_img}}" mode="aspectFill" data-gid="{{item.id}}" bindtap="goToDetaile"></image>
        <view class="title ellipsis">{{item.subject}}</view>
        <view class="tag ellipsis">{{item.type_name}} | {{item.area_name}} | {{item.house_price}}</view>
        <view class="headPic pa" bindtap="goToDesigner" data-did="{{item.id}}">
            <image src="{{item.cover_img}}"></image>
            <text class="ellipsis tc">{{item.designer_name}}</text>
        </view>
    </view>
    <view wx:if="{{hasMore}}">
        <button type="warn" plain="true" disabled="{{disabled}}" bindtap="getMore" class="mt20">{{moreTxt}}</button>
    </view>
</view>
<include src="../../template/footer.wxml"/>
